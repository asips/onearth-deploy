diff --git a/docker/local-deployment/docker-compose.local.yml b/docker/local-deployment/docker-compose.local.yml
index 5d67f662..505db9dc 100644
--- a/docker/local-deployment/docker-compose.local.yml
+++ b/docker/local-deployment/docker-compose.local.yml
@@ -17,9 +17,9 @@ services:
     hostname: onearth-time-service
     platform: ${DOCKER_PLATFORM_OPTION}
     image: nasagibs/onearth-time-service:${ONEARTH_IMAGE_TAG}
-    depends_on:
-      onearth-deps:
-        condition: service_completed_successfully
+    #depends_on:
+    #  onearth-deps:
+    #    condition: service_completed_successfully
     build:
       args: 
         ONEARTH_VERSION: $ONEARTH_VERSION
@@ -31,21 +31,22 @@ services:
     environment: 
       DEBUG_LOGGING: $DEBUG_LOGGING
       S3_URL: /onearth/mrf-archive
-      FORCE_TIME_SCRAPE: true
+      FORCE_TIME_SCRAPE: "true"
     networks:
       - oe2
     restart: always
     volumes:
       - ${CONFIG_DIR:-./onearth-configs}/config:/etc/onearth/config
       - ${MRF_ARCHIVE_DIR:-./local-mrf-archive}:/onearth/mrf-archive:ro
+      - ./docker-compose.local.yml:/.dockerenv:ro
   onearth-tile-services:
     container_name: onearth-tile-services
     hostname: onearth-tile-services
     platform: ${DOCKER_PLATFORM_OPTION}
     image: nasagibs/onearth-tile-services:${ONEARTH_IMAGE_TAG}
-    depends_on:
-      onearth-deps:
-        condition: service_completed_successfully
+    #depends_on:
+    #  onearth-deps:
+    #    condition: service_completed_successfully
     build:
       args: 
         ONEARTH_VERSION: $ONEARTH_VERSION
@@ -59,11 +60,12 @@ services:
       SERVER_NAME: $SERVER_NAME
       DEBUG_LOGGING: $DEBUG_LOGGING
       S3_URL: /onearth/mrf-archive
-      IDX_SYNC: true
+      IDX_SYNC: "true"
     volumes: 
       - ./certs:/home/oe2/onearth/certs
       - ${CONFIG_DIR:-./onearth-configs}/config:/etc/onearth/config
       - ${MRF_ARCHIVE_DIR:-./local-mrf-archive}:/onearth/mrf-archive:ro
+      - ./docker-compose.local.yml:/.dockerenv:ro
     networks:
       - oe2
     restart: always
@@ -72,9 +74,9 @@ services:
     hostname: onearth-capabilities
     platform: ${DOCKER_PLATFORM_OPTION}
     image: nasagibs/onearth-capabilities:${ONEARTH_IMAGE_TAG}
-    depends_on:
-      onearth-deps:
-        condition: service_completed_successfully
+    #depends_on:
+    #  onearth-deps:
+    #    condition: service_completed_successfully
     build:
       args: 
         ONEARTH_VERSION: $ONEARTH_VERSION
@@ -92,14 +94,15 @@ services:
     volumes:
       - ${CONFIG_DIR:-./onearth-configs}/config:/etc/onearth/config
       - ${MRF_ARCHIVE_DIR:-./local-mrf-archive}:/onearth/mrf-archive:ro
+      - ./docker-compose.local.yml:/.dockerenv:ro
   onearth-reproject:
     container_name: onearth-reproject
     hostname: onearth-reproject
     platform: ${DOCKER_PLATFORM_OPTION}
     image: nasagibs/onearth-reproject:${ONEARTH_IMAGE_TAG}
     depends_on:
-      onearth-deps:
-        condition: service_completed_successfully
+      #onearth-deps:
+      #  condition: service_completed_successfully
       onearth-time-service:
         condition: service_healthy
       onearth-tile-services:
@@ -122,14 +125,15 @@ services:
     volumes:
       - ${CONFIG_DIR:-./onearth-configs}/config:/etc/onearth/config
       - ${MRF_ARCHIVE_DIR:-./local-mrf-archive}:/onearth/mrf-archive:ro
+      - ./docker-compose.local.yml:/.dockerenv:ro
   onearth-wms:
     container_name: onearth-wms
     hostname: onearth-wms
     platform: ${DOCKER_PLATFORM_OPTION}
     image: nasagibs/onearth-wms:${ONEARTH_IMAGE_TAG}
     depends_on:
-      onearth-deps:
-        condition: service_completed_successfully
+      #onearth-deps:
+      #  condition: service_completed_successfully
       onearth-time-service:
         condition: service_healthy
       onearth-tile-services:
@@ -154,6 +158,7 @@ services:
       - ${CONFIG_DIR:-./onearth-configs}/config:/etc/onearth/config
       - ${MRF_ARCHIVE_DIR:-./local-mrf-archive}:/onearth/mrf-archive:ro
       - ${SHP_ARCHIVE_DIR:-./local-shp-archive}:/onearth/shp-archive:ro
+      - ./docker-compose.local.yml:/.dockerenv:ro
     networks:
       - oe2
     restart: always
@@ -165,8 +170,8 @@ services:
     ports:
       - ${ONEARTH_PORT:-80}:80
     depends_on:
-      onearth-deps:
-        condition: service_completed_successfully
+      #onearth-deps:
+      #  condition: service_completed_successfully
       onearth-time-service:
         condition: service_healthy
       onearth-tile-services:
@@ -187,6 +192,7 @@ services:
       - ${CONFIG_DIR:-./onearth-configs}/config:/etc/onearth/config:ro
       - ${MRF_ARCHIVE_DIR:-./local-mrf-archive}:/onearth/mrf-archive:ro
       - ${SHP_ARCHIVE_DIR:-./local-shp-archive}:/onearth/shp-archive:ro
+      - ./docker-compose.local.yml:/.dockerenv:ro
     networks:
       - oe2
     restart: always
